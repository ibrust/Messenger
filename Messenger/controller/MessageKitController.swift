//
//  MessageKitController.swift
//  Messenger
//
//  Created by Field Employee on 11/15/20.
//

import Foundation
import MessageKit


/*
class Message_Kit_Controller : MessagesViewController, MessagesDataSource, MessagesLayoutDelegate, MessagesDisplayDelegate {
    
    // I don't know how you'd change the sender... there's no indexPath ... maybe it's always me?
    func currentSender() -> SenderType {
        
    }
    
    // I think this is the equivalent of cellforrowat
    func messageForItem(at indexPath: IndexPath, in messagesCollectionView: MessagesCollectionView) -> MessageType {
        
        // I believe the MessageType is already defined... same with Sender ... 
 
    }
    
    func numberOfSections(in messagesCollectionView: MessagesCollectionView) -> Int {
        
    }
    
    override func viewDidLoad() {
        super.viewDidLoad()
        messagesCollectionView.messagesDataSource = self
        messagesCollectionView.messagesLayoutDelegate = self
        messagesCollectionView.messagesDisplayDelegate = self
    }
    
    
    
}


*/
